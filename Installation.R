# Step 1: Please check your current R version, R version 4.0 or greater is required.
  version

# Step 2: Installation
  install.packages("shiny")
  install.packages("tidyverse")
  install.packages("parallel")
  install.packages("parallel")
  install.packages("bslib")
  install.packages("DT")
  
  
